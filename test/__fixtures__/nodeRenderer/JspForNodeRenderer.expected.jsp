<c:forEach var="item" items="${  }" varStatus="loop">COPY</c:forEach>
<c:forEach var="keyAndItem" items="${  }" varStatus="loop"><c:set var="key" value="${ keyAndItem.key }" /><c:set var="item" value="${ keyAndItem.value }" />BREAKPOINT</c:forEach>
<c:forEach var="item" items="${  }" varStatus="loop"></c:forEach>
